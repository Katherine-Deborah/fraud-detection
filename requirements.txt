# Fraud Detection Pipeline — dependencies
#
# Pinned incrementally, session by session (mirrors docker-compose.yml).
# Session-specific libs (kafka-python, feast, apache-airflow, mlflow, torch,
# fastapi, etc.) are added in the session that first needs them, not all at
# once up front.

# Dev tooling
pytest==8.3.4
python-dotenv==1.0.1
ruff==0.8.4

# Session 1: synthetic dataset generation + EDA
# pandas pinned to <3 as of Session 3 -- feast==0.65.0 requires
# `pandas<3,>=1.4.3`. Confirmed the Session 1 generator and EDA scripts
# still run identically under 2.3.3 (regenerated the full 5M-row dataset,
# same row count and fraud rate as the original 3.0.5 run).
pandas==2.3.3
numpy==2.4.6
pyarrow==25.0.0
matplotlib==3.11.1

# Session 2: Kafka ingestion (producer/consumer)
confluent-kafka==2.6.1

# Session 3: Feature store (Feast + Redis)
feast[redis]==0.65.0

# Session 4: Airflow orchestration + data validation
# (Airflow itself runs in its own container -- see Dockerfile.airflow /
# requirements-airflow.txt -- this is just for local iteration/testing of
# the Great Expectations suite in validation/.)
great_expectations==1.19.1

# Session 5: local model training + MLflow tracking
# Class imbalance handled via class weighting (not SMOTE) for all 4
# models -- see docs/model_comparison.md for the per-model rationale --
# so no imbalanced-learn dependency is needed.
scikit-learn==1.4.2
mlflow==2.12.2
# torch: pinned to the CUDA 12.1 build already installed locally (GTX 1650
# Ti). On a machine without that GPU/CUDA setup, install the CPU wheel
# instead: `pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cpu`
torch==2.5.1
