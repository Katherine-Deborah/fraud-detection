# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
.venv/
.venv-sagemaker/
.venv-registry/
.venv-serving/
venv/
env/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/

# Environment / secrets
.env
.env.*
!.env.example
*.pem
*.key
aws_credentials
.aws/

# Docker
*.log

# IDE
.vscode/
.idea/
*.swp

# Claude Code local session state
.claude/

# OS
.DS_Store
Thumbs.db

# Data / model artifacts (regenerated, not committed)
data/
*.parquet
*.csv
!docs/**/*.csv
mlruns/
mlartifacts/
*.pt
*.pth
*.onnx
*.pkl
*.joblib
serving/_export_staging/

# Session 9: Prometheus multiprocess metrics dir (serving/run_server.py)
# and generated Evidently drift reports (dags/fraud_pipeline_dag.py) --
# both regenerated at runtime, not committed.
serving/_prom_multiproc/
monitoring/drift_reports/

# Terraform
infra/**/.terraform/
infra/**/*.tfstate
infra/**/*.tfstate.*
infra/**/.terraform.lock.hcl
infra/**/*.tfvars

# Kubernetes local state
k8s/**/*.local.yaml

# Feast (regenerate via `feast apply` -- see feature_store/feature_repo/)
feature_store/feature_repo/registry.db
